Usage
=====

wdmapper can be used both, as :doc:`command line application <cli>` and as
:doc:`Python library <api>`. This document first describes how to install 
and invoke wdmapper from the command line.

